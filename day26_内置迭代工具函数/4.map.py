"""
date 2026/1/7
@author chryl

1. map()：映射转换
核心功能
将一个函数应用于一个或多个可迭代对象的每个元素，返回迭代器，包含函数处理后的结果。
若传入多个可迭代对象，函数需接收对应数量的参数，且迭代以最短序列为准。

语法
map(function, iterable, ...)

2.迭代工具函数对比表
函数	                核心作用	                输入要求	                        输出类型
map()	            函数映射处理元素	        函数 + 1 个 / 多个可迭代对象	    迭代器
filter()	        按条件筛选元素	        判断函数 + 可迭代对象	            迭代器
reversed()	        反向遍历元素	            支持反向的序列	                迭代器
iter()	            转换可迭代对象为迭代器	    可迭代对象	                    迭代器
range()	            生成整数序列	            起始 / 结束 / 步长	            迭代器（Python3）
sorted()	        排序元素并返回列表	        可迭代对象	                    列表
"""
# 案例1：单个可迭代对象（数值平方）
nums = [1, 2, 3, 4]
result = map(lambda x: x ** 2, nums)
print(list(result))  # 输出: [1, 4, 9, 16]

# 案例2：多个可迭代对象（元素相加）
a = [1, 2, 3]
b = [10, 20, 30]
result2 = map(lambda x, y: x + y, a, b)
print(list(result2))  # 输出: [11, 22, 33]